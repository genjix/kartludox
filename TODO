shade other buttons when toggle fold/call/raise
css everything
fix -1,-1 message
rewrite to MVC
note editor that is autosaving depending on the user set
view -> menu for each dock window
settings -> hide menubar, hide sidebar, hide toolbar, full screen mode, show statusbar
load table layout from config positions incl stylesheets also.
icons and prettiness
